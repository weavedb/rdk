{
  "name": "weavedb-rdk",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "run-rollup": "cd node && sudo docker-compose -f docker-compose.yml up --build",
    "envoy": "cd node && sudo docker-compose -f envoy-local.yml up --build"
  },
  "dependencies": {}
}
